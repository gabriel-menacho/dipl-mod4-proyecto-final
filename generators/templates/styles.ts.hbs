import { Theme } from '@mui/material';

const ns = '{{kebabCase name}}';

export const {{camelCase name}}Classes = {
  root: `${ns}__root`,
};

const styles = {
  {{camelCase name}}: ({ theme }: { theme: Theme }) => ({}),
};

export default styles;
