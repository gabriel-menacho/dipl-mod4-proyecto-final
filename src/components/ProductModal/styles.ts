import { Theme } from '@mui/material';

const ns = 'product-modal';

export const productModalClasses = {
  root: `${ns}__root`,
};

const styles = {
  productModal: ({ theme }: { theme: Theme }) => ({}),
};

export default styles;
