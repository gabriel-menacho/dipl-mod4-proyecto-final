import { Theme } from '@mui/material';

const ns = 'table';

export const tableClasses = {
  root: `${ns}__root`,
};

const styles = {
  table: ({ theme }: { theme: Theme }) => ({}),
};

export default styles;
