import { Theme } from '@mui/material';

const ns = 'confirmation-dialog';

export const confirmationDialogClasses = {
  root: `${ns}__root`,
};

const styles = {
  confirmationDialog: ({ theme }: { theme: Theme }) => ({}),
};

export default styles;
