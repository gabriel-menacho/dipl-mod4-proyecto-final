import { Theme, boxClasses, modalClasses } from '@mui/material';

const ns = 'login-modal';

export const loginModalClasses = {
  root: `${ns}__root`,
};

const styles = {
  loginModal: ({ theme }: { theme: Theme }) => ({}),
};

export default styles;
