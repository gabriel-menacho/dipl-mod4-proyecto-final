import { Theme } from '@mui/material';

const ns = 'input';

export const inputClasses = {
  root: `${ns}__root`,
};

const styles = {
  input: ({ theme }: { theme: Theme }) => ({}),
};

export default styles;
